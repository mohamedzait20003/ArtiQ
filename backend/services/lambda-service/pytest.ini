[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Use prepend mode to ensure imports work correctly
pythonpath = .
# Use importlib mode for better import handling
# This ensures the lambda-service directory is treated as the root
consider_namespace_packages = true
